<%= form_for @order do |f| %>
  <%= render "shared/error_messages", object: f.object %>

  <%= f.label :transaction_date, "transaction_date" %><br>
  <div>
    <%= l Date.today, format: :long %>
  </div>

  <%= f.submit yield(:button_text), class: "btn btn-primary" %>
<% end %>
